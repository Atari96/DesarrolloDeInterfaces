@*<summary>
    Formulario con campos para introducir las direcciones del cliente y entrega
    En los campos de formulario incluimos el mensaje de validación ValidationMessage
    En la parte del codigo el parametro que pasamos a la pagina son los correspondientes a la clase address
</sumary*@
    
<!-- En los campos de formulario incluimos el mensaje de validación ValidationMessage-->
<div class="form-field">
    <label>Nombre:</label>
    <div>
        <InputText @bind-Value="Address.Name" />
        <ValidationMessage For="@(() => Address.Name)" />
    </div>
</div>

<div class="form-field">
    <label>Dirección 1:</label>
    <div>
        <InputText @bind-Value="Address.Line1" />
        <ValidationMessage For="@(() => Address.Line1)" />
    </div>
</div>

<div class="form-field">
    <label>Teléfono:</label>
    <div>
        <InputText @bind-Value="Address.Line2" />
        <ValidationMessage For="@(() => Address.Line2)" />
    </div>
</div>

<div class="form-field">
    <label>Ciudad:</label>
    <div>
        <InputText @bind-Value="Address.City" />
        <ValidationMessage For="@(() => Address.City)" />
    </div>
</div>

<div class="form-field">
    <label>Region:</label>
    <div>
        <input @bind="Address.Region" />
    </div>
</div>

<div class="form-field">
    <label>Código postal:</label>
    <div>
        <InputText @bind-Value="Address.PostalCode" />
        <ValidationMessage For="@(() => Address.PostalCode)" />
    </div>
</div>

@code {
    // El parametro que pasamos a la pagina son los correspondientes a la clase address
    [Parameter, EditorRequired] public Address Address { get; set; } = new();
}
